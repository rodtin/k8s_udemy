apiVersion: v1
kind: Pod
metadata:
        name: nginx
        labels:
                app: nginx-pod
                type: front-end

spec:
        containers:
                - name: nginx-container
                  image: nginx

